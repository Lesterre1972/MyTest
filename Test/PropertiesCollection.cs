using System.Collections.Generic;

namespace Test
{
    internal enum PropertyType

    {
        Id,
        Name,
        CssSelector,
    }


 /*   internal class PropertiesCollection
    {
        public Dictionary<string, object> urlToPages = new Dictionary<string, object>();

        public PropertiesCollection()
        {
            urlToPages.Add(",
                new RegisterPageObject());

            //Added third page for My account
            urlToPages.Add("http://automationpractice.com/index.php?controller=my-account/",
                new MyAccountPageObject());
        }
    }*/
}